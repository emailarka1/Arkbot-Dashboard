# Fix: Folder Management Issues — Implementation Plan

## 3 Fixes

### Fix 1: FolderSelector — handle both response formats
`FolderSelector.tsx` line 39: `setFolders(data.folders || [])`
Change to: `setFolders(data.folders || data.files || [])`
This handles both old workflow (`{ files: [...] }`) and new workflow (`{ folders: [...] }`).

### Fix 2: DriveManager — filter out folders from file list
`DriveManager.tsx` — add filter to only show files (items with extensions), not folders.
Folder items should only show as folder cards, not in the file table.

### Fix 3: DriveManager — orphan detection for folders
Folders shouldn't be compared against database entries. Only files should be marked as "tanpa DB".

---

## Files to Modify

| File | Change |
|------|--------|
| `src/components/drive/FolderSelector.tsx` | Handle `data.folders \|\| data.files` |
| `src/components/drive/DriveManager.tsx` | Filter folders from file list, fix orphan detection |

## Verification
1. Dropdown folder selector shows folders (even with old n8n workflow)
2. File list in Drive tab doesn't show folders
3. "Tanpa DB" only for files, not folders
4. "Tanpa Drive" only for files that have drive_file_id but not in Drive
